# This file is no longer used.
#
# The nginx configuration is now in nginx/templates/default.conf.template.
# The nginx Docker image's envsubst mechanism renders it into conf.d/ at
# container startup, substituting NGINX_MAX_BODY_MIB from the environment.
#
# See docker-compose.yml and .env.example for the variable definition.
