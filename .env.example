# Copy to .env and fill in your values before running docker compose up.

# ── Upload auth ───────────────────────────────────────────────────────────────

# Token callers must supply to upload.
# Leave empty (or remove) for open uploads.
UPLOAD_TOKEN=changeme

# Set to false to allow open uploads even when UPLOAD_TOKEN is configured.
# Useful to temporarily disable auth without forgetting the token.
# Default: true when UPLOAD_TOKEN is set, false when it is empty.
REQUIRE_AUTH=true

# ── File limits ───────────────────────────────────────────────────────────────

# Maximum upload size in MiB. Default: 256.
MAX_FILESIZE=256

# Nginx client_max_body_size in MiB. Must equal MAX_FILESIZE + 2.
# The +2 covers multipart form overhead so nginx never rejects a valid upload
# before the Go backend does. Update both values together.
NGINX_MAX_BODY_MIB=258

# ── Retention ─────────────────────────────────────────────────────────────────

# Maximum number of days a file is kept (applies to tiny files). Default: 30.
MAX_FILE_AGE=30

# Minimum number of days a file is kept (applies to max-size files). Default: 3.
MIN_FILE_AGE=3

# ── Admin ─────────────────────────────────────────────────────────────────────

# Password for the /admin panel.
ADMIN_PASSWORD=changeme

# Contact email shown on the index page.
ADMIN_EMAIL=you@example.com

# Timezone for log timestamps (e.g. Europe/Amsterdam, America/New_York).
TZ=UTC
